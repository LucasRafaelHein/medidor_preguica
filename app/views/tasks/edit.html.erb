<div class="task-form-card">
  <h2>Editar Tarefa</h2>

  <%= form_with(model: @task, local: true) do |form| %>
    <div>
      <%= form.label :task, "Descrição" %>
      <%= form.text_field :task %>
    </div>

    <div class="actions">
      <%= form.submit "Atualizar", class: "btn btn-submit" %>
      <%= link_to "Voltar", tasks_path, class: "btn btn-back" %>
    </div>
  <% end %>
</div>
